/**
 * Helpers
 * 
 * Quick helper methods from lodash, these can be extracted later if we don't want any lodash dependencies
 * 
 */

// _merge is a fast way to handle 'deep-merge' of objects instead of the Object.assign, which is a shallow merge.
// import _merge from 'lodash/merge'
// export { _merge }
